# Digital Asset Links Placeholder

`assetlinks.json` will be added later after the Android Trusted Web Activity
wrapper exists and the signing certificate data is known.

Planned Android package name:

```text
com.satomi.finance
```

Do not guess `assetlinks.json` yet. The file must contain the real Android
package name and SHA-256 signing certificate fingerprint from the production
signing key.
